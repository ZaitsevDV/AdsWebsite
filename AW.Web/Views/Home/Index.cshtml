<div id="content">
    @if (HttpContext.Current.User.Identity.IsAuthenticated)
    {
        <h3>
            @Html.ActionLink("Logout", "Logout", "Login")<br/>
            @Html.ActionLink("Admin Contact", "Contact")
        </h3>
    }
    else
    {
        <h2>
            @Html.ActionLink("Login", "Login", "Login")
        </h2>
    }
</div>